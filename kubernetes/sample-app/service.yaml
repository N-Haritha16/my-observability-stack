# kubernetes/sample-app/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: flask-app
  namespace: observability-demo
  labels:
    app: flask-app
spec:
  type: ClusterIP
  selector:
    app: flask-app
  ports:
    - name: http
      port: 5000
      targetPort: 5000